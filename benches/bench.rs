// Criterion benchmarks for performance-critical paths.
// Groups: poly_mul (Mode A and Mode B), keygen, encaps, decaps.
// Run with: cargo bench
// Each benchmark covers all three parameter sets (HQC-128, HQC-192, HQC-256).
